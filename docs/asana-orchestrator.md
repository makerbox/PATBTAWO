# PATBTAWO Asana Compatibility

The old Asana-specific script is now a compatibility entry point for the
provider-agnostic PATBTAWO project. New setups should use:

```sh
python -m patbtawo --provider asana
```

Use the main runbook: [task-orchestrator.md](task-orchestrator.md).
